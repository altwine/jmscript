package test

/*
//func ge() -> number {
//	player_send_message("test", "CONCATENATION")
//}

func de(arg: number) -> number {
	if arg == 1 {
		return 2
	} else {
		return 3
	}
}

func t() {
	a := 1

	b := de(a)
	_ :: b
}
*/

event player_join() {
	a := 1

	b := a % 2

	if b == 1 {
		player_send_message("test", "CONCATENATION")
	}

	if b == 1 {
		player_send_message("test2", "CONCATENATION")
	}
}
