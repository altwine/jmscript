package test

event player_join() {
    test_var := "TestTestTest"

    if if_variable_text_contains(test_var, "Test") {
        player_send_message("default_player", "Success")
    } else {
        player_send_message("default_player", "Failure")
    }
}
